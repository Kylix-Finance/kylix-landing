import Section from "~/components/Section";

const MarketTrends = () => {
  return (
    <Section
      heading={{
        left: "Market",
        right: "Trends",
      }}
      description="The new cross-chain Lending Platform, on Polkadot."
    >
      <div>MarketTrends</div>
    </Section>
  );
};

export default MarketTrends;
